# Architecture Deep Dive

## 1. The Holographic Principle

The engine is built on a single axiom:

> Oi = Pi(R)
> 
> The Observer is a Projection of Reality.

This means there is no "objective view from nowhere." Every measurement is a projection. The dashboard does not display "the universe" — it displays one valid projection of the symbolic field.

## 2. The Dual Projection Problem

The system maintains two simultaneous projections:

- Local (Flat) Projection: Cartesian, grid-based, observer-centric
- Global (Spherical) Projection: Holistic, curvature-aware, field-centric

The Dim-Switch metric (0.0 to 1.0) tracks the balance between these two modes.

## 3. The Resonance Engine

The core is not a physics simulation. It is a symbolic resonance field:

coherence = 0.55 + 0.25*sin(t*0.3) + 0.05*sin(t*0.7)
stability = 0.70 + 0.15*sin(t*0.15) + 0.03*cos(t*0.4)

These are not arbitrary. The coefficients encode:
- 0.55 — baseline coherence (slightly above chaos)
- 0.70 — baseline stability (strong equilibrium)
- 0.3 / 0.7 — primary/secondary harmonic frequencies
- 0.15 / 0.4 — stability damping and recovery rates

## 4. The Calendar Lattice

Time is not a line. It is a geometric lattice:

365 Days    ->  Outer ring (Obelisks)   -> Information carriers
52 Phases   ->  Middle ring (Syrens)     -> Harmonic gates
12 Large    ->  Inner ring (Pyramids)    -> Structural anchors

Each ring rotates at a different angular velocity, creating interference patterns that the dashboard visualizes as live motion.

## 5. Why 1.42 GHz?

1.42 GHz (21 cm) is the hyperfine transition frequency of neutral hydrogen — the most abundant element in the universe. It is the universal carrier wave. By locking the engine's symbolic clock to this frequency, the system aligns its computational rhythm with the cosmos's native broadcast channel.

## 6. The 7 Invariants

These are non-negotiable constraints. If any invariant is violated, the audit fails:

1. Finite — All state spaces are bounded [0,1]
2. Deterministic — Same input -> Same output (no hidden randomness)
3. Conservation Zero — Total symbolic mass is preserved
4. Phase Domain — Phase index always in [1,52]
5. Coherence Domain — Coherence always in [0,1]
6. Stability Domain — Stability always in [0,1]
7. No Silent Transport — No information moves without leaving an audit trace

## 7. Why Pure Python stdlib?

- Sovereignty — Zero external dependencies = zero supply-chain attacks
- Portability — Runs on Termux, Raspberry Pi, servers, anything with Python
- Transparency — Every line is inspectable. No black boxes.
- Speed — For a symbolic engine, Python is more than fast enough. The bottleneck is the observer, not the computer.

Document Version: v4.0-eng
Classification: UCA Sovereign Technical
